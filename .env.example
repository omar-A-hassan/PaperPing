# ── Required ──────────────────────────────────────────────────────────────────
YOUR_PHONE_NUMBER=+1234567890        # your own iMessage number

# ── LLM Provider (choose one) ─────────────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-...         # default provider

# LLM_PROVIDER=openrouter            # uncomment to use OpenRouter instead
# OPENROUTER_API_KEY=sk-or-...
# LLM_MODEL=claude-sonnet-4-5        # optional — overrides the default model
